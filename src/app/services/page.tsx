/**
 * app/services/page.tsx — Services Overview
 * GIGW 3.0 · WCAG 2.2 AA
 * A dedicated page to browse, filter, and search all government services.
 */
import { redirect } from "next/navigation";

export default function ServicesPage() {
    redirect("/chat");
}
